# YouTube Downloader — Dependências
# Instale com: pip install -r requirements.txt

yt-dlp>=2024.1.1        # Download do YouTube (substitui pytube)
pyinstaller>=6.0.0      # Geração do .exe para Windows
